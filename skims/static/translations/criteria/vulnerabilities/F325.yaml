src.lib_path.f325.iam_is_role_over_privileged:
  en: >
    IAM Role is miss configured:

    - Should not allow * action on its trust policy
    - Should not allow * action on its permissions policy
    - Should not allow Allow+NotPrincipal in its trust policy
    - Should not allow * resource with PassRole action on its permissions policy
    - Should not allow * resource on its permissions policy
    - Should not allow Allow+NotAction on trust permissions
    - Should not allow Allow+NotAction
    - Should not allow Allow+NotResource
    - Should not have AdministratorAccess policy
  es: >
    El Role de IAM está mal configurado:

    - No debe permitir acciones * sobre su política de confianza
    - No debe permitir acciones * en su política de permisos
    - No debe permitir Allow + NotPrincipal en su política de confianza
    - No debe permitir recursos * con acción PassRole en su política de permisos
    - No debe permitir recursos * en su política de permisos
    - No debe permitir Allow + NotAction en permisos de confianza
    - No debe permitir Allow + NotAction
    - No debe permitir Allow + NotResource
    - No debe tener una política de AdministratorAccess
src.lib_path.f325.iam_has_privileges_over_iam:
  en: >
    Policies have privileges over IAM
  es: >
    Las políticas tienen privilegios sobre IAM
src.lib_path.f325.iam_is_policy_miss_configured:
  en: >
    IAM Policy should consider the next rules:

    - Should not allow * action.
    - Should not apply directly to users, Should be on group.
    - Should not allow * resource with PassRole action.
    - Should not allow * resource.
    - Should not allow Allow+NotAction.
    - Should not allow Allow+NotResource.
  es: >
    La política de IAM debe considerar las siguientes reglas:

    - No debe permitir acción *.
    - No debe aplicarse directamente a los usuarios, debe estar en el grupo.
    - No debe permitir recurso * con acción PassRole.
    - No debe permitir recurso *.
    - No debe permitir Allow + NotAction.
    - No debe permitir Allow + NotResource.
src.lib_path.f325.iam_has_wildcard_resource_on_write_action:
  en: >
    Write actions are allowed on all resources
  es: >
    Acciones de escritura estan permitidas para todos los recursos
src.lib_path.f325.kms_key_has_master_keys_exposed_to_everyone:
  en: >
    Amazon KMS master keys are exposed to everyone
  es: >
    Las llaves de Amazon KMS están expuestas a todos
