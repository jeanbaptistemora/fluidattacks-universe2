---
language: EN
namespace: namespace
output:
  file_path: skims/test/lib_dast/outputs/{FINDING}.csv
  format: CSV
checks:
  - {FINDING}
dast:
  aws_credentials:
    - access_key_id: "000f"
      secret_access_key: "000f"
