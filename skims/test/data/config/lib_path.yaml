language: EN
namespace: namespace
output: skims/test/outputs/lib_path.csv
path:
  include:
    - skims/test/data/lib_path
    - skims/test/data/lib_root

    # Include and exclude skims to test the filter
    - integrates/
  exclude:
    - glob(integrates/**/*)
timeout: 3600
