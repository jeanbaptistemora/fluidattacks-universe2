[blankdiv]
<div class="{role? {role}}">|</div>

[paragraph]
<div class="paragraph{role? {role}}{unbreakable-option? unbreakable}"{id? id="{id}"}>{title?<h2>{title}</h2>}<p>
|
</p></div>

[blockdef-div]
delimiter=^=$
template=blankdiv
options=sectionbody
posattrs=style

[blockdef-div2]
delimiter=^==$
template=blankdiv
options=sectionbody
posattrs=style

[blockdef-div3]
delimiter=^===$
options=sectionbody
template=blankdiv
posattrs=style
