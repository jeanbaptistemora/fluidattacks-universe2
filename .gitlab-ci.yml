image: registry.gitlab.com/fluidsignal/web

before_script:
  - cd content/
  - if egrep -r 'Fluid|Fluidsignal\ Group|fluidsignal'; then echo "El único nombre aceptado es FLUID"; exit 1; fi
  - cd ../

deploy:
  environment: production	
  script:
    - pelican --fatal errors content/
    - mv output/en/blog-en output/en/blog && mv output/es/blog-es output/es/blog
    - aws s3 rm --recursive s3://blog.fluid.la
    - aws s3 cp --recursive --acl=public-read-write output/ s3://blog.fluid.la
  only:
     - master

test:
  script:
    - pelican --fatal errors content/
  except:
    - master
