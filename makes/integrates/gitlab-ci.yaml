/deployTerraform/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /deployTerraform/integratesBackups
  stage: deploy-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/dirOfModules/integrates:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/dirOfModules/integrates
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintTerraform/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintTerraform/integratesBackups
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/pipelineOnGitlab/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /pipelineOnGitlab/integratesBackups
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testTerraform/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /testTerraform/integratesBackups
  stage: test-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
