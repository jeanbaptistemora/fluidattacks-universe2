/deployTerraform/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /deployTerraform/integratesBackups
  stage: deploy-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/deployTerraform/integratesResources:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /deployTerraform/integratesResources
  stage: deploy-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/deployTerraform/integratesSecrets:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /deployTerraform/integratesSecrets
  stage: deploy-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/dirOfModules/integrates:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/dirOfModules/integrates
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/dirOfModules/integratesBackChartsGenerators:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/dirOfModules/integratesBackChartsGenerators
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/imports/integrates:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/imports/integrates
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/integratesBackDeployPermissionsMatrix:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/module/integratesBackDeployPermissionsMatrix
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/integratesBackMigrations:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/module/integratesBackMigrations
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/integratesBackTestsE2e:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintPython/module/integratesBackTestsE2e
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintTerraform/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintTerraform/integratesBackups
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintTerraform/integratesResources:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintTerraform/integratesResources
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintTerraform/integratesSecrets:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /lintTerraform/integratesSecrets
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/pipelineOnGitlab/integrates:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /pipelineOnGitlab/integrates
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testTerraform/integratesBackups:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /testTerraform/integratesBackups
  stage: test-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testTerraform/integratesResources:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /testTerraform/integratesResources
  stage: test-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testTerraform/integratesSecrets:
  image: ghcr.io/fluidattacks/makes:21.09
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /testTerraform/integratesSecrets
  stage: test-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
