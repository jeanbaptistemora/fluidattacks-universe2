apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrates-master
  namespace: production
spec:
  replicas: ${REPLICAS}
  selector:
    matchLabels:
      app: integrates-master
  strategy:
    rollingUpdate:
      maxSurge: 10%
      maxUnavailable: 10%
  template:
    metadata:
      labels:
        app: integrates-master
        # This ensures that the resource is updated on every apply
        uuid: ${UUID}
    spec:
      nodeSelector:
        worker_group: production
      containers:
        - name: integrates
          image: ghcr.io/fluidattacks/makes:21.11
          imagePullPolicy: Always
          command: [sh]
          args:
          - -c
          - m f /integrates/back/deploy/probes/liveness;
            m f /integrates/back/deploy/probes/readiness;
            m f /integrates/back prod
          env:
          - name: K8S_COMPAT
            value: "1"
          - name: INSTANA_AGENT_HOST
            valueFrom:
              fieldRef:
                fieldPath: status.hostIP
          resources:
            # Ensure your request and limit fits in the node:
            #   kubectl get node --selector=worker_group=production
            #   kubectl describe node <node>
            #
            # You can have 1 pod per node, or 2, or 3, etc..
            #
            # Debug if pods were actually able to use the node:
            #   kubectl get pod -n production
            #   kubectl describe pod -n production <pod>
            requests:
              cpu: 1000m
              memory: 2000Mi
            limits:
              cpu: 2000m
              memory: 3800Mi
          envFrom:
            - secretRef:
                name: integrates-master
          ports:
            - containerPort: 8001
          readinessProbe:
            exec:
              command:
              - sh
              - -c
              - K8S_COMPAT=1
                ~/.makes/out-integrates-back-deploy-probes-readiness/bin/*
                aws_login_prod integrates-prod
                FluidIntegrates http://localhost:8001
            initialDelaySeconds: 120
            periodSeconds: 60
            successThreshold: 1
            failureThreshold: 10
            timeoutSeconds: 60
          livenessProbe:
            exec:
              command:
              - sh
              - -c
              - K8S_COMPAT=1
                ~/.makes/out-integrates-back-deploy-probes-liveness/bin/*
                aws_login_prod integrates-prod
                FluidIntegrates http://localhost:8001
                https://app.fluidattacks.com
            initialDelaySeconds: 120
            periodSeconds: 60
            successThreshold: 1
            failureThreshold: 10
            timeoutSeconds: 60
