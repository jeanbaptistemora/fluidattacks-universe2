apiVersion: v1
kind: Secret
metadata:
  name: integrates-${CI_COMMIT_REF_NAME}
  namespace: development
type: Opaque
data:
  CI_COMMIT_REF_NAME: ${B64_CI_COMMIT_REF_NAME}
  CI_COMMIT_SHA: ${B64_CI_COMMIT_SHA}
  DEV_AWS_ACCESS_KEY_ID: ${B64_DEV_AWS_ACCESS_KEY_ID}
  DEV_AWS_SECRET_ACCESS_KEY: ${B64_DEV_AWS_SECRET_ACCESS_KEY}
