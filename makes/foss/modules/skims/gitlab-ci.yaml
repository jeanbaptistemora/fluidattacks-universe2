/computeOnAwsBatch/skimsOwaspBenchmarkAndUpload:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE != "schedule"
      when: never
    - if: $skims_benchmark_on_aws == null
      when: never
    - when: always
  script:
    - m . /computeOnAwsBatch/skimsOwaspBenchmarkAndUpload
  stage: scheduler
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/deployContainerImage/skimsProd:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH != "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /deployContainerImage/skimsProd
  stage: deploy-app
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/deployTerraform/skims:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /deployTerraform/skims
  stage: deploy-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/dirOfModules/skims:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/imports/skims:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/imports/skims
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/skimsProcessGroup:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/module/skimsProcessGroup
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/skimsTest:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/module/skimsTest
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/skimsTestMocksHttp:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/module/skimsTestMocksHttp
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintPython/module/skimsTestSdk:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/module/skimsTestSdk
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/lintTerraform/skims:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintTerraform/skims
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/securePythonWithBandit/skims:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /securePythonWithBandit/skims
  stage: lint-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/skims/test/cli:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/test/cli
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/skims/test/sdk:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/test/sdk
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_cmdi:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_cmdi
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_crypto:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_crypto
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_hash:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_hash
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_ldapi:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_ldapi
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_pathtraver:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_pathtraver
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_securecookie:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_securecookie
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_sqli:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_sqli
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_trustbound:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_trustbound
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_weakrand:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_weakrand
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_xpathi:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_xpathi
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@benchmark_xss:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_xss
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@functional:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@functional
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@instance_references:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@instance_references
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@lib_apk:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_apk
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@lib_http:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_http
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@lib_path:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_path
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@lib_ssl:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_ssl
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f001:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f001
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f004:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f004
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f008:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f008
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f021:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f021
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f063:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f063
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f107:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f107
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@unittesting:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@unittesting
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@vulnerable_js_app:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@vulnerable_js_app
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testPython/skims@vulnerableapp:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@vulnerableapp
  stage: test-code
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
/testTerraform/skims:
  image: ghcr.io/fluidattacks/makes:21.10
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testTerraform/skims
  stage: test-infra
  tags:
    - autoscaling
  variables:
    GIT_DEPTH: 1
