apiVersion: v1
kind: Service
metadata:
  name: integrates-master
  namespace: production
spec:
  type: NodePort
  ports:
    - name: integrates
      port: 81
      targetPort: 8001
      protocol: TCP
  selector:
    app: integrates-master
