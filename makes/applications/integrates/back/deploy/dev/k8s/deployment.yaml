apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrates-__DEPLOYMENT_NAME__
  namespace: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: integrates-__DEPLOYMENT_NAME__
  template:
    metadata:
      labels:
        app: integrates-__DEPLOYMENT_NAME__
    spec:
      nodeSelector:
        worker_group: development
      containers:
        - name: integrates
          image: nixos/nix:2.3.6
          imagePullPolicy: Always
          command: [sh]
          args:
            - -c
            - nix-env -i cachix &&
              cachix use fluidattacks &&
              nix-env
                -i integrates-back
                -f __CI_PROJECT_URL__/-/archive/__CI_COMMIT_SHA__.tar.gz &&
              integrates-back eph
          resources:
            requests:
              cpu: 600m
              memory: 2000Mi
            limits:
              cpu: 4000m
              memory: 5000Mi
          env:
            - name: DEPLOYMENT_DATE
              value: "__DATE__"
          envFrom:
            - secretRef:
                name: integrates-__DEPLOYMENT_NAME__
          ports:
            - containerPort: 8001
          readinessProbe:
            exec:
              command:
              - sh
              - -c
              - nix-env
                  -i integrates-back-probes-readiness
                  -f __CI_PROJECT_URL__/-/archive/__CI_COMMIT_SHA__.tar.gz &&
                integrates-back-probes-readiness
                  integrates-dev
                  FluidIntegrates
                  http://localhost:8001
            initialDelaySeconds: 0
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 40
            timeoutSeconds: 60
          livenessProbe:
            exec:
              command:
              - sh
              - -c
              - nix-env
                  -i integrates-back-probes-liveness
                  -f __CI_PROJECT_URL__/-/archive/__CI_COMMIT_SHA__.tar.gz &&
                integrates-back-probes-liveness
                  integrates-dev
                  FluidIntegrates
                  http://localhost:8001
                  https://__BRANCH__.integrates.fluidattacks.com
            initialDelaySeconds: 0
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 40
            timeoutSeconds: 60
        - name: db
          image: nixos/nix:2.3.6
          imagePullPolicy: Always
          command: [sh]
          args:
            - -c
            - nix-env -i cachix &&
              cachix use fluidattacks &&
              nix-env
                -i integrates-db
                -f __CI_PROJECT_URL__/-/archive/__CI_COMMIT_SHA__.tar.gz &&
              integrates-db
          resources:
            requests:
              cpu: 600m
              memory: 2000Mi
            limits:
              cpu: 4000m
              memory: 3000Mi
          env:
            - name: DEPLOYMENT_DATE
              value: "__DATE__"
          envFrom:
            - secretRef:
                name: integrates-__DEPLOYMENT_NAME__
        - name: cache
          image: nixos/nix:2.3.6
          imagePullPolicy: Always
          command: [sh]
          args:
            - -c
            - nix-env -i cachix &&
              cachix use fluidattacks &&
              nix-env
                -i integrates-cache
                -f __CI_PROJECT_URL__/-/archive/__CI_COMMIT_SHA__.tar.gz &&
              integrates-cache
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 600m
              memory: 2000Mi
            limits:
              cpu: 4000m
              memory: 3000Mi
          env:
            - name: DEPLOYMENT_DATE
              value: "__DATE__"
          envFrom:
            - secretRef:
                name: integrates-__DEPLOYMENT_NAME__
        - name: storage
          image: nixos/nix:2.3.6
          imagePullPolicy: Always
          command: [sh]
          args:
            - -c
            - nix-env -i cachix &&
              cachix use fluidattacks &&
              nix-env
                -i integrates-storage
                -f __CI_PROJECT_URL__/-/archive/__CI_COMMIT_SHA__.tar.gz &&
              integrates-storage
          resources:
            requests:
              cpu: 600m
              memory: 2000Mi
            limits:
              cpu: 4000m
              memory: 3000Mi
          env:
            - name: DEPLOYMENT_DATE
              value: "__DATE__"
          envFrom:
            - secretRef:
                name: integrates-__DEPLOYMENT_NAME__
