#! /usr/bin/env nix-shell
#!   nix-shell -i bash
#!   nix-shell --option restrict-eval false
#!   nix-shell --option sandbox false
#!   nix-shell --show-trace
#!   nix-shell melts.nix
#  shellcheck shell=bash
export PYTHONWARNINGS="ignore"

export PYTHONPATH="${pyPkgMelts}/site-packages/:${PYTHONPATH}"

export PATH="${pyPkgMelts}/site-packages/bin/:${PATH}"

"${pyPkgMelts}/site-packages/bin/melts" "$@"
