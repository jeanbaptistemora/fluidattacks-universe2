apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: etcd-restore-operator
    chart: etcd-operator-0.8.0
    heritage: Tiller
    release: vault
  name: vault-etcd-operator-etcd-restore-operator
  namespace: serves
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: etcd-restore-operator
      release: vault
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: etcd-restore-operator
        release: vault
      name: vault-etcd-operator-etcd-restore-operator
    spec:
      containers:
      - command:
        - etcd-restore-operator
        env:
        - name: MY_POD_NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        - name: MY_POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: SERVICE_ADDR
          value: etcd-restore-operator:19999
        image: quay.io/coreos/etcd-operator:v0.9.2
        imagePullPolicy: Always
        name: etcd-restore-operator
        ports:
        - containerPort: 19999
          protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 100m
            memory: 128Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      serviceAccount: vault-etcd-operator-etcd-restore-operator
      serviceAccountName: vault-etcd-operator-etcd-restore-operator
      terminationGracePeriodSeconds: 30
