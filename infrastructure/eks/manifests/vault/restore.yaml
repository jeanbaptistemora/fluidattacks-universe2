apiVersion: "etcd.database.coreos.com/v1beta2"
kind: "EtcdRestore"
metadata:
  name: etcd-cluster
spec:
  etcdCluster:
    name: etcd-cluster
  backupStorageType: S3
  s3:
    path: ${VAULT_S3_BUCKET}/vault.etcd.backup-${DATE}
    awsSecret: aws
