import { StyleSheet } from "react-native";

export const styles: Dictionary = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  loadingGif: {
    height: 100,
    resizeMode: "stretch",
    width: 100,
  },
});
