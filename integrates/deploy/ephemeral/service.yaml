apiVersion: v1
kind: Service
metadata:
  name: integrates-__DEPLOYMENT_NAME__
  namespace: ephemeral
spec:
  type: NodePort
  ports:
    - name: integrates1
      port: 80
      targetPort: 8000
      protocol: TCP
    - name: integrates2
      port: 81
      targetPort: 8001
      protocol: TCP
  selector:
    app: integrates-__DEPLOYMENT_NAME__
