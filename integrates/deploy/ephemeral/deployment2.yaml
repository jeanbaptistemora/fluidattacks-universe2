apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrates2-__DEPLOYMENT_NAME__
  namespace: ephemeral
spec:
  replicas: 1
  selector:
    matchLabels:
      app: integrates2-__DEPLOYMENT_NAME__
  template:
    metadata:
      labels:
        app: integrates2-__DEPLOYMENT_NAME__
    spec:
      nodeSelector:
        worker_group: large
      containers:
        - name: integrates2
          image: registry.gitlab.com/fluidattacks/product/app2:__DEPLOYMENT_NAME__
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 250m
              memory: 512Mi
          env:
            - name: DEPLOYMENT_DATE
              value: "__DATE__"
          envFrom:
            - secretRef:
                name: integrates-__DEPLOYMENT_NAME__
          ports:
            - containerPort: 8080
          readinessProbe:
            exec:
              command: [bash, deploy/ephemeral/integrates2-probes.sh, run_readiness_probe]
            initialDelaySeconds: 60
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 40
          livenessProbe:
            exec:
              command: [bash, deploy/ephemeral/integrates2-probes.sh, run_liveness_probe]
            initialDelaySeconds: 60
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 40
