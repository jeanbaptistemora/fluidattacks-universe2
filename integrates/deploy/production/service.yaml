apiVersion: v1
kind: Service
metadata:
  name: integrates-master
  namespace: production
spec:
  type: NodePort
  ports:
    - name: integrates1
      port: 80
      targetPort: 8000
      protocol: TCP
    - name: integrates2
      port: 81
      targetPort: 8001
      protocol: TCP
  selector:
    app: integrates-master
