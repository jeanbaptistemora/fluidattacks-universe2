"Checkout type definition"
type Checkout {
  "Redirect url when checkout is cancelled"
  cancelUrl: String!
  "Stripe payment url"
  paymentUrl: String!
  "Redirect url when checkout is successful"
  successUrl: String!
}
