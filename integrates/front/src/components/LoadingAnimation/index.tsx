import styled from "styled-components";

import loading from "resources/loading.gif";

const LoadingAnimation = styled.img.attrs({
  alt: "Loading animation",
  className: "db center",
  src: loading,
})``;

export { LoadingAnimation };
