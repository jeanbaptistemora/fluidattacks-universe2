check that page contains "Continue with Google"
check that page contains "Continue with Microsoft"
check that page contains "Continue with Bitbucket"
press "Continue with Google"
wait 3 sec
check that page contains "Sign in"
