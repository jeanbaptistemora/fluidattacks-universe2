open URL "https://{CI_COMMIT_REF_NAME}.app.fluidattacks.com/orgs/okada/groups/unittesting/vulns"
check that page contains "014. Insecure functionality"
press "014. Insecure functionality"
check that page contains "192.168.1.19"
check that page contains "192.168.1.20"
check that page contains "Filter"
press "Filter"
select "Vulnerable" from "currentState"
check that page contains "192.168.1.19"
check that page does not contain "192.168.1.20"
press "close-filters"
press "192.168.1.19"
press "modal-close"
press "Reattack"
check that page contains "Justification"
press "Cancel"
check that page contains "Please select vulnerabilities to reattack"
press "Reattack"
check that page contains "Justification"
enter "This is a reattack test justification" into edit "treatmentJustification"
press "Confirm"
check that page contains "A reattack has been requested successfully"
