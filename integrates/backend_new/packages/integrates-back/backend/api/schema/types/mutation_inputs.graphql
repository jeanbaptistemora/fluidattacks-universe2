input EnvironmentInput {
  "Environment URL"
  urlEnv: String!
}

input ExecutionVulnerabilitiesInput {
  "acepted vulnerabilities"
  accepted: [ExploitResultInput!]
  "closed exploits"
  closed: [ExploitResultInput!]
  "open vulnerabilities"
  open: [ExploitResultInput!]
}

input ExploitResultInput {
  "CVSS Exploitability"
  exploitability: Float
  "Execution kind"
  kind: String
  "Vulnerability state"
  state: VulnerabilityState
  "Vulnerability where"
  where: String
  "Execution specific"
  who: String
}

input GitRootFilterInput {
  "Paths to be filtered"
  paths: [String!]!
  "Filter policy for the specified paths"
  policy: GitRootFilterPolicy!
}
