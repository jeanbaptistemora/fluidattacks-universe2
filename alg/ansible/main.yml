- hosts: all
  gather_facts: false
  tasks:
    - raw: which python || apt-get update
    - raw: which python || apt-get install -y python python-apt

- name: Execute ALG role
  hosts: alg
  roles:
    - name: Install alg
      role: alg
