[tool.poetry]
name = "tap_mixpanel"
version = "0.1.0"
description = ""
authors = ["Product Team <development@fluidattacks.com>"]

[tool.poetry.dependencies]
python = "^3.7"
requests = "2.25.1"

[tool.poetry.dev-dependencies]
mypy = "0.790"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
tap-mixpanel = 'tap_mixpanel.__init__:main'
