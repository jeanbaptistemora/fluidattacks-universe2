# Standard libraries
# Third party libraries
# Local libraries
from streamer_zoho_crm.core.bulk import _bulk


create_bulk_job = _bulk.create_bulk_job
update_all = _bulk.update_all
get_bulk_data = _bulk.get_bulk_data
