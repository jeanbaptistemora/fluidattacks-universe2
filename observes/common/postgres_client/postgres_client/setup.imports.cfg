[importlinter]
  root_package = postgres_client

[importlinter:contract:dag]
  name = root_dag
  type = layers
  layers =
    schema
    table
    client
    cursor
    connection
    utils
  containers =
    postgres_client
