[importlinter]
  root_package = postgres_client

[importlinter:contract:dag]
  name = root_dag
  type = layers
  layers =
    schema
    table
    data_type
    client
    cursor
    connection
    utils
  containers =
    postgres_client
