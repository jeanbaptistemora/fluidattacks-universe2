[importlinter]
  root_package = code_etl

[importlinter:contract:root]
  name = root_dag
  type = layers
  layers =
    upload_repo
    amend
    compute_bills
    migration
    client
    mailmap
  containers =
    code_etl
