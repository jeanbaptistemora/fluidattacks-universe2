---
/deployTerraform/observes:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /deployTerraform/observes
  stage: deploy-infra
  tags:
    - prod_observes_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesArch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesArch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesCommonPaginator:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesCommonPaginator
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesCommonPostgresClient:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesCommonPostgresClient
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesCommonSingerIo:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesCommonSingerIo
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesServiceTimedoctorTokens:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesServiceTimedoctorTokens
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapAnnounceKit:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapAnnounceKit
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapBugsnag:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapBugsnag
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapCsv:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapCsv
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapDelighted:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapDelighted
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapFormstack:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapFormstack
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapJson:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapJson
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapMailchimp:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapMailchimp
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapMixpanel:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapMixpanel
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/observesTapTimedoctor:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/imports/observesTapTimedoctor
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesCommonPaginator:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesCommonPaginator
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesCommonPostgresClient:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesCommonPostgresClient
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesCommonPostgresClientTests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesCommonPostgresClientTests
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesCommonSingerIo:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesCommonSingerIo
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesCommonSingerIoTests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesCommonSingerIoTests
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesServiceTimedoctorTokens:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesServiceTimedoctorTokens
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesStreamerZohoCrm:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesStreamerZohoCrm
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesStreamerZohoCrmTests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesStreamerZohoCrmTests
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapAnnounceKit:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapAnnounceKit
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapAnnounceKitTests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapAnnounceKitTests
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapBugsnag:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapBugsnag
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapCsv:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapCsv
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapCsvTests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapCsvTests
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapDelighted:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapDelighted
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapFormstack:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapFormstack
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapJson:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapJson
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapMailchimp:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapMailchimp
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapMailchimpTests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapMailchimpTests
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTapTimedoctor:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTapTimedoctor
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/observesTargetRedshift:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintPython/module/observesTargetRedshift
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintTerraform/observes:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /lintTerraform/observes
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/asm-dal/check/arch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/asm-dal/check/arch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/asm-dal/check/runtime:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/asm-dal/check/runtime
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/asm-dal/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/asm-dal/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/asm-dal/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/asm-dal/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/asm-dal/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/asm-dal/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/singer-io/test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/singer-io/test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/utils-logger-2/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/utils-logger-2/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/utils-logger-2/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/utils-logger-2/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/common/utils-logger-2/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/common/utils-logger-2/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/etl/code/lint:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/etl/code/lint
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/etl/code/test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/etl/code/test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/etl/dynamo_etl_conf/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/etl/dynamo_etl_conf/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/etl/dynamo_etl_conf/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/etl/dynamo_etl_conf/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/etl/dynamo_etl_conf/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/etl/dynamo_etl_conf/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/batch-stability/check/arch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/batch-stability/check/arch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/batch-stability/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/batch-stability/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/batch-stability/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/batch-stability/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/batch-stability/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/batch-stability/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/job-last-success/check/arch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/job-last-success/check/arch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/job-last-success/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/job-last-success/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/job-last-success/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/job-last-success/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/job-last-success/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/job-last-success/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/jobs-scheduler/check/arch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/jobs-scheduler/check/arch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/jobs-scheduler/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/jobs-scheduler/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/jobs-scheduler/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/jobs-scheduler/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/service/jobs-scheduler/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/service/jobs-scheduler/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-announcekit/fx-test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-announcekit/fx-test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-announcekit/test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-announcekit/test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-checkly/check/arch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-checkly/check/arch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-checkly/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-checkly/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-checkly/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-checkly/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-checkly/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-checkly/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-csv/test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-csv/test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-dynamo/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-dynamo/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-dynamo/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-dynamo/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-dynamo/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-dynamo/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-gitlab/check/arch:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-gitlab/check/arch
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-gitlab/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-gitlab/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-gitlab/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-gitlab/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-gitlab/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-gitlab/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-mailchimp/test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-mailchimp/test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/tap-mixpanel/test:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/tap-mixpanel/test
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/target-s3/check/tests:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/target-s3/check/tests
  stage: test-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/target-s3/check/types:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/target-s3/check/types
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/observes/singer/target-s3/env/dev:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /observes/singer/target-s3/env/dev
  stage: build
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/pipelineOnGitlab/observes:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /pipelineOnGitlab/observes
  stage: lint-code
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testTerraform/observes:
  image: ghcr.io/fluidattacks/makes:22.08
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|observes)/
  script:
    - m . /testTerraform/observes
  stage: test-infra
  tags:
    - dev_small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
